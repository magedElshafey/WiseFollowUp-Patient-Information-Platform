export interface HeroAbout {
    heading: string
    description: string[];
    anas_image: {
        title: string,
        image: string
    },
    khaled_image: {
        title: string,
        image: string
    }, is_active: boolean
}