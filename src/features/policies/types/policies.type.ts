export interface Policies {
    data: {
        id: number,
        title: string,
        slug: string,
        description: string,
        content: string,
        created_at: string,
        updated_at: string,
    }[]
}